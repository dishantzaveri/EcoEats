import 'package:flutter/material.dart';

class ByteScreen extends StatelessWidget {
  const ByteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Byte'),
      ),
      body: const Center(
        child: Text('Byte'),
      ),
    );
  }
}
